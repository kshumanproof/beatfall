node_modules/
.vercel/
.env
.env.*
!.env.example
.DS_Store
*.log
_transfer.zip
_testdata/
_to_delete/
handoff/

# Built by test/mkstub.js from public/app.html every run. A committed copy of
# app.html is exactly how the repo ends up with two of them and nobody knows
# which one shipped.
test/app.html
test/stub.html
test/server/api/
# Built by test/pages.js from the real page in public/, same reasoning.
test/stub-*.html

# Screenshots and scratch output from working sessions. Useful while a design
# is being decided, and about six megabytes of PNGs that nothing in the product
# reads. Kept on disk, out of the repo.
Claude outputs/
